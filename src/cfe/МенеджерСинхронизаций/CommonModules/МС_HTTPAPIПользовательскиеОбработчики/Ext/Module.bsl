
#Область ПрограммныйИнтерфейс

// Выполняет программную выборку данных по имени обработчика из настройки API.
// Добавьте собственные ветки по ИмяМетода или вызовите общий модуль конфигурации.
//
// Параметры:
//  ИмяМетода - Строка - значение из справочника настроек API.
//  Параметры - Структура - параметры запроса (внешние и служебные).
//
// Возвращаемое значение:
//  Произвольный - данные для сериализации (ТаблицаЗначений, Массив структур и т.д.).
//
Функция ВыполнитьОбработчик(ИмяМетода, Параметры) Экспорт
	
	Если Не ЗначениеЗаполнено(ИмяМетода) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// Пример: раскомментируйте и реализуйте по необходимости
	//Если ИмяМетода = "МойОбработчик" Тогда
	//	Возврат ...;
	//КонецЕсли;
	
	Возврат Неопределено;
	
КонецФункции

#КонецОбласти
